"""Telegram command and message handlers, grouped by concern.

  access — joining and administration (/start, /invite, /users, /kick)
  audio  — the core flow: /help, cookies upload, link -> audio file
  files  — managing stored files (/list, /delete, bare-number delete)
"""
